<div id='payments'>
  <div class='back_title'>
    <%= link_to 'back', '/groups' %>
    <h1>Payments</h1>
  </div>

  <% @payments.each do |payment| %>
      <div class='payment'>
        <p><%= payment.amount %></p>
        <p><%= payment.name %></p>
        <p><%= payment.created_at.strftime('%d-%b-%Y') %></p>
      </div>
  <% end %>

  <%= link_to 'Add new payment', '/payments/new' %>
</div>